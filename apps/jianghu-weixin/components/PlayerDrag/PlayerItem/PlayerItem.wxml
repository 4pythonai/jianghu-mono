<view catchtap="itemClick">
  <view class="player-order-item">
    <view class="drag-handle">
      <text class="drag-icon">⋮⋮</text>
    </view>
    <view>
      <text
        class="player-dot"
      >{{(redBlueConfig === '4_固拉' && (abcd === '1' || abcd === '2')) || (redBlueConfig !== '4_固拉' && (abcd === '1' || abcd === '4')) ? '🔵' : '🔴'}}</text>
      <text class="player-serial">#{{abcd}}</text>
    </view>
    <view class="player-avatar-wrap">
      <PlayerAvatar
        avatar="{{itemData.avatar}}"
        userId="{{itemData.userid}}"
        avatarOnly="{{true}}"
        size="small"
      />
    </view>
    <text class="player-name">{{itemData.nickname || '未知玩家'}}</text>
  </view>
</view>
