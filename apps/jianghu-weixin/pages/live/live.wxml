<view class="container">
  <!-- 导航栏容器（带背景图，高度为屏幕1/4） -->
  <view class="navbar-wrapper" style="height: {{navbarWrapperHeight}}px;">
    <!-- 背景图片 -->
    <image src="/images/traning.png" mode="aspectFill" class="navbar-bg-image"></image>
    <!-- 导航栏 -->
    <navBar
      extClass="navbar-transparent"
      background="transparent"
      backgroundColorTop="transparent"
      logoSrc="/assets/logos/logo.png"
      logoWidth="200rpx"
      logoHeight="132px"
      searchBar="{{true}}"
      searchText="搜索"
      searchWidth="187rpx"
      bindsearch="handleSearch"
    />
  </view>
  <!-- Tab切换区域 -->
  <view class="tab-container">
    <view class="tab-header">
      <view
        class="tab-item {{currentTab === 0 ? 'active' : ''}}"
        bindtap="switchTab"
        data-index="0"
      >我的比赛</view>
      <view
        class="tab-item {{currentTab === 1 ? 'active' : ''}}"
        bindtap="switchTab"
        data-index="1"
      >广场</view>
    </view>
    <!-- Tab内容区域 -->
    <view class="tab-content">
      <view class="tab-pane" hidden="{{currentTab !== 0}}">
        <!-- 我的比赛 -->
        <GameLists games="{{games}}" />
      </view>
      <view class="tab-pane" hidden="{{currentTab !== 1}}">
        <!-- 广场内容 -->
        <GameLists games="{{games}}" showEmpty="{{true}}" />
      </view>
    </view>
  </view>
</view>
