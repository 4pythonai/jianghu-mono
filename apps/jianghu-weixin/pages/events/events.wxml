<wxs src="/utils/image.wxs" module="image" />
<view class="container">
  <!-- 轮播Banner区域 -->
  <view class="banner-section">
    <swiper
      class="banner-swiper"
      autoplay="{{true}}"
      interval="{{4000}}"
      duration="{{500}}"
      circular="{{true}}"
      bindchange="onBannerChange"
    >
      <swiper-item wx:for="{{banners}}" wx:key="id" bindtap="onBannerTap" data-banner="{{item}}">
        <image class="banner-image" src="{{image.imageUrl(item.image)}}" mode="aspectFill" />
      </swiper-item>
    </swiper>
    <!-- 自定义轮播指示器 -->
    <view class="banner-indicator" wx:if="{{banners.length > 1}}">
      <view
        wx:for="{{banners}}"
        wx:key="id"
        class="indicator-dot {{currentBannerIndex === index ? 'active' : ''}}"
      />
    </view>
  </view>

  <!-- Tab切换区域 -->
  <view class="tab-container">
    <view class="tab-header">
      <view
        class="tab-item {{currentTab === 0 ? 'active' : ''}}"
        bindtap="switchTab"
        data-index="0"
      >赛事招募</view>
      <view
        class="tab-item {{currentTab === 1 ? 'active' : ''}}"
        bindtap="switchTab"
        data-index="1"
      >已报名赛事</view>
    </view>

    <!-- Tab内容区域 -->
    <view class="tab-content">
      <!-- 加载中 -->
      <view class="loading-wrapper" wx:if="{{loading}}">
        <view class="loading-spinner"></view>
        <text class="loading-text">加载中...</text>
      </view>

      <!-- 赛事列表 -->
      <view class="event-list" wx:else>
        <GameLists games="{{eventList}}" showEmpty="{{true}}" router="events_menu" />
      </view>
    </view>
  </view>
</view>
