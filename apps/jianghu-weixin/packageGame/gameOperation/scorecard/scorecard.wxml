<!--成绩卡页面-->
<CustomNavBar title="成绩卡" />
<view class="container">
    <view class="page-content">
        <web-view 
            wx:if="{{showWebView}}"
            src="{{webviewUrl}}" 
            bindmessage="onWebViewMessage"
            binderror="onWebViewError"
            bindload="onWebViewLoad">
        </web-view>
    </view>
</view>