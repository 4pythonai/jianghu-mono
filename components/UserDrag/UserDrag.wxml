<!-- UserDrag 组件 - 用户拖拽排序组件 -->
<view class="user-drag-container">
  <view
    class="user-item {{draggingIndex === index ? 'dragging' : ''}} {{dragging ? 'dragging-active' : ''}}"
    wx:for="{{currentUserList}}"
    wx:key="id"
    data-index="{{index}}"
    bind:touchstart="onTouchStart"
    catch:touchmove="onTouchMove"
    catch:touchend="onTouchEnd"
  >
    <userItem index="{{index}}" item-data="{{item}}" bind:click="onItemClick" />
  </view>
</view>
