<view class="page-container">
	<CourtSelector
		course-info="{{ selectedCourse }}"
		title="选择半场"
		show-confirm-button="{{ true }}"
		bind:confirm="onCourtConfirm"
		bind:select="onCourtSelect"
		bind:error="onError"
	/>
</view>